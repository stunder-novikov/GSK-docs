# Custom Categories

Categories are defined in Project Settings -> Categories as an array of FName. Default: Display, Graphics, Audio, Input, Gameplay.

Add your own categories (e.g. Accessibility, Network) -- they will appear in the dropdown when editing Data Assets.
